interface:
  display_name: "Modular Scaffold Skill"
  short_description: "Build self-contained Go business modules"
  default_prompt: "Use $modular to scaffold one Go Application with self-contained bounded-context modules, wire its cmd composition root, and verify contract boundaries."
