// internal/{{SVC}}/app/{{SURFACE}}/{{METHOD_SNAKE}}.go - {{METHOD_PASCAL}} implementation.
package {{SURFACE}}

import (
	"context"

	common "{{GO_PACKAGE}}"
)

func (s *Server) {{METHOD_PASCAL}}(ctx context.Context, req *common.{{METHOD_PASCAL}}Request) (*common.{{METHOD_PASCAL}}Response, error) {
	_ = s
	_ = ctx
	_ = req

	return &common.{{METHOD_PASCAL}}Response{}, nil
}
