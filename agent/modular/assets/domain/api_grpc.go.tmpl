// internal/{{DOMAIN}}/api/{{SURFACE}}/grpc.go - interface surface gRPC adapter.
// Tokens: {{DOMAIN}}, {{SURFACE}}, {{SERVICE_PASCAL}}, {{GO_PACKAGE}}
package {{SURFACE}}

import (
	"google.golang.org/grpc"

	common "{{GO_PACKAGE}}"
)

// RegisterGRPC wires this interface surface onto a gRPC server.
// cmd/ passes this to rpcserver.NewServer(cfg, RegisterGRPC, opts...).
func RegisterGRPC(s grpc.ServiceRegistrar, svc common.{{SERVICE_PASCAL}}Server) {
	common.Register{{SERVICE_PASCAL}}Server(s, svc)
}
