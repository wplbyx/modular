// internal/{{DOMAIN}}/app/{{SURFACE}}/server.go - app surface server.
// Tokens: {{DOMAIN}}, {{SURFACE}}, {{SERVICE_PASCAL}}, {{PROJECT}}, {{GO_PACKAGE}}
package {{SURFACE}}

import (
	common "{{GO_PACKAGE}}"
	"{{PROJECT}}/internal/{{DOMAIN}}/domain"
)

// Server implements the generated pb service for this interface surface.
// It coordinates use cases and depends on domain ports, not infra adapters.
type Server struct {
	common.Unimplemented{{SERVICE_PASCAL}}Server

	queries  domain.QueryRepository
	commands domain.CommandRepository
}

func NewServer(queries domain.QueryRepository, commands domain.CommandRepository) *Server {
	return &Server{
		queries:  queries,
		commands: commands,
	}
}

var _ common.{{SERVICE_PASCAL}}Server = (*Server)(nil)
