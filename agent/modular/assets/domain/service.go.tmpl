// internal/{{DOMAIN}}/service/{{SURFACE}}.go - optional protocol/use-case adapter.
// Tokens: {{DOMAIN}}, {{SURFACE}}, {{SERVICE_PASCAL}}, {{PROJECT}}, {{GO_PACKAGE}}
package service

import (
	common "{{GO_PACKAGE}}"
	surfaceapp "{{PROJECT}}/internal/{{DOMAIN}}/app/{{SURFACE}}"
)

// Service is optional. Prefer app/{{SURFACE}}.Server directly unless this
// adapter hides real versioning, protocol mapping, or facade complexity.
type Service struct {
	common.Unimplemented{{SERVICE_PASCAL}}Server

	app *surfaceapp.Server
}

func NewService(app *surfaceapp.Server) *Service {
	return &Service{app: app}
}

var _ common.{{SERVICE_PASCAL}}Server = (*Service)(nil)
